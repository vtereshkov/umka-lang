import (
    "parse.um"    
)

fn main() {       
    var p: parse.Parser
    res := p.exec("((lambda (x y) (cons (car x) y)) (quote (a b)) (quote(c d)))")
    //res := p.exec("((lambda () (cons (quote a) (quote b))) )")
    printf("%s\n", res)
}