fn test*() {
	x := -1 + atan2(2, 7) + sqrt(2) * trunc(3.6) - 1 + log(exp(3)) - round(7)
	y := round(sin(3.14)) + 8 / 3

	b := 'b' >= 'a' 
	c := fabs(sqrt(3) - 1.7) < 0.1

	printf("%v %v %v %v\n", x, y, b, c)
}

fn main() {
	test()
}
