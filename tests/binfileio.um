import "std.um"

fn write(name, s: str) {
	f := std.fopen(name, "wb")
	std.fwrite(f, &[]char(s))
	std.fclose(f)	
} 

fn read(name: str): str {
	f := std.fopen(name, "rb")
	data, err := std.freadall(f)
	std.exitif(err)
	std.fclose(f)

	return str(data)
}

fn test*() {
	name := "fio.txt"
	write(name, "Hello World")
	printf(read(name) + '\n')
	std.remove(name)
}

fn main() {
	test()
}