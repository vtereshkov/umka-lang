import "import/std.um"

const (
    s = "Hello"
    slen = len(s)
)

fn main() {
    t := s + " World"
    std.println(std.itoa(slen) + " " + std.itoa(len(s)) + " " + std.itoa(len(t)))
    std.println("ok")
    
    var a: [10] int    
    //const alen = len(a)    
    std.println(std.itoa(len(a)))
}
    
    